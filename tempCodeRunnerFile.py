while True:
    #     cursor.execute('SELECT sifra_termina FROM termin')
    #     sve_sifre_termina = [sif[0] for sif in cursor.fetchall()]
    #     print(sve_sifre_termina)
    #     random_slova = ''.join(random.choices(string.ascii_uppercase, k=2))
        